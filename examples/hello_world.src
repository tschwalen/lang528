#
# Obligatory 'Hello World' included so you can actually run the example from README
#

function main (argv)
    if argv.length() < 1
        print( "Hello, World!");
        return 0;
    else
        let i = 0;
        while i < argv.length()
            print( "Hello, " + argv[i] + "!");
            i += 1;
        ..
    ..
..